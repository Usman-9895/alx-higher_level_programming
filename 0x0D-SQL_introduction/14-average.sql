-- Calcule la moyenne des scores de tous les enregistrements dans
-- la table second_table de la base de données courante.
-- Le nom de la colonne de résultat doit être moyen.

SELECT AVG(`score`) AS `average` FROM `second_table`;
